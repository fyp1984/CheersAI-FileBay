# CheersAI-FileBay - Docker

Dockerfile is found in the root of the repository.

This repository does not assume a public Docker image is available. Build your own image from source.

If you are starting from upstream Docker practices, review your internal security baseline first (rootless, minimal image, non-root user, read-only filesystem where possible).
